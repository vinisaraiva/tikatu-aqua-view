services:
  - type: web
    name: tikatu-aqua-view
    runtime: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html